import React from 'react';
import CardsList from "@/view/CardsList/CardsList";

const Homepage = () => {
    return (
        <div>
            <CardsList />
        </div>
    );
};

export default Homepage;